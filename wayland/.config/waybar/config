{
	"layer": "top",
	"position": "top",

    "height": 20,
    // "width": ,

    "spacing": 0,

    "modules-left": ["hyprland/workspaces", "hyprland/submap"],
    "modules-center": ["disk", "custom/cputemp", "cpu", "custom/gpu", "memory"],
    "modules-right": ["bluetooth", "tray", "network", "wireplumber", "custom/weather", "battery", "clock"],

	"hyprland/workspaces": {
	},

	"hyprland/submap": {
		"tooltip": false
	},

	"cpu": {
		"format": "{usage:2}%",
		"interval": 10,
		"tooltip": false
	},

	"custom/cputemp": {
		"exec": "~/.config/waybar/cputemp.sh",
		"interval": 10,
		"tooltip": false
	},

	"disk": {
		"format": "{path} {percentage_used}%" ,
		"interval": 30,
		"tooltip": false
	},
	
	"memory": {
		"format": "MEM {percentage:2}%",
		"interval": 10,
		"tooltip": false
	},

	"custom/gpu": {
		"exec": "~/.config/waybar/gpustats.sh",
		"interval": 10,
		"tooltip": false
	},

	"bluetooth": {
		// TODO, once I actually need to use bluetooth
	},

	"tray": {
		"icon-size": 16,
		"spacing": 10,
		"reverse-direction": true,
		"show-passive-items": true
	},

    "network": {
        "format-wifi": "",
        "format-ethernet": "󰈀",
        "format-disconnected": "",
		"tooltip": false,
		"on-click": "nm-connection-editor",
    },

	"wireplumber": {
		"format": "{icon}",
		"format-muted": "",
		"tooltip": false,
        "on-click": "pavucontrol",
		"format-icons": ["","",""]
	},

	"custom/weather": {
		"exec": "~/.config/waybar/weather.py",
		"interval": 60,
		"tooltip": false
	},

	// TODO
    "battery": {
        "states": {
            "warning": 25,
        },
        "format": "{capacity}% {icon}",
        "format-alt": "{time} {icon}",
        "format-full": "",
        "format-icons": ["", "", "", "", ""]
    },

    "clock": {
		"format": "{:%b %d %I:%M}"
    },
}
